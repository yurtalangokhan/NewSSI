"""Agent flow canvas domain layer.

Holds the component template registry, the dynamic option sources, and the
flow validator. Nothing here performs graph compilation — that lives in
``agents/graphs/flow_builder.py``.
"""
